[pytest]
# Restrict test discovery to the tests directory and only files matching the standard pattern.
testpaths = tests
python_files = test_*.py
# Ensure pytest-asyncio plugin is loaded for async fixtures.
plugins = pytest_asyncio
# Prevent recursion into unexpected directories.
norecursedirs = .* build dist *.egg-info .venv
asyncio_mode = auto
